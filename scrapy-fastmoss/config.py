# -*- coding: utf-8 -*-
"""
FastMoss 汽车与摩托车板块 爬虫配置文件
"""

import os
from datetime import datetime

# ============================================================
# 登录信息
# ============================================================
FASTMOSS_PHONE = "14700094800"
FASTMOSS_PASSWORD = "aw8508104.."

# ============================================================
# 爬取目标
# ============================================================
# 区域
REGION = "US"

# 一级分类: 汽车与摩托车
L1_CID = 23
L1_NAME = "汽车与摩托车"

# 二级分类列表（11 个子分类）
# 设为 None 表示爬取该一级分类下的所有商品（不按子分类拆分）
# 设为列表则逐个子分类爬取，可以突破单类 500 页上限
SUBCATEGORIES = [
    {"l2_cid": 930184, "name": "内部配件"},
    {"l2_cid": 940296, "name": "汽车修理工具"},
    {"l2_cid": 930185, "name": "外部配件"},
    {"l2_cid": 930056, "name": "汽车零部件"},
    {"l2_cid": 809488, "name": "摩托车配件"},
    {"l2_cid": 940936, "name": "摩托车零部件"},
    
    {"l2_cid": 930056, "name": "汽车零部件"},
    {"l2_cid": 809488, "name": "摩托车配件"},
    {"l2_cid": 940936, "name": "摩托车零部件"},
    # ---- 以下子分类在浏览器中进一步确认后补充 ----
    # 运行时会自动发现并补充遗漏的子分类
    #https://www.fastmoss.com/zh/e-commerce/search?region=US&page=1&l1_cid=23&l2_cid=940296
    # 如已知可手动添加，格式: {"l2_cid": 数字, "name": "名称"}
]

# 是否在运行时自动发现所有子分类（推荐开启）
AUTO_DISCOVER_SUBCATEGORIES = True

# 每页条数（10, 20, 50）
PAGE_SIZE = 10

# 最大页数上限（FastMoss 限制 500 页）
MAX_PAGES = 500

# ============================================================
# 速度控制 — 防止触发验证码
# ============================================================
# 翻页间隔（秒）
PAGE_DELAY_MIN = 4
PAGE_DELAY_MAX = 8

# 切换子分类间隔（秒）
CATEGORY_DELAY = 5

# 最大重试次数
MAX_RETRIES = 3

# ============================================================
# 网络超时
# ============================================================
PAGE_TIMEOUT = 60000       # 页面加载超时 (ms)
API_TIMEOUT = 30000        # API 等待超时 (ms)
DATA_WAIT_TIMEOUT = 30     # 等待数据出现的最长秒数

# ============================================================
# 输出配置
# ============================================================
OUTPUT_DIR = os.path.join(os.path.dirname(__file__), "data", "automotive")
TIMESTAMP = datetime.now().strftime("%Y%m%d_%H%M%S")

# ============================================================
# 浏览器设置
# ============================================================
HEADLESS = False  # 首次运行建议 False，方便手动过验证码
BROWSER_STATE_DIR = os.path.join(os.path.dirname(__file__), "browser_state")

# ============================================================
# FastMoss URLs
# ============================================================
BASE_URL = "https://www.fastmoss.com"
SEARCH_URL = f"{BASE_URL}/zh/e-commerce/search"
LOGIN_URL = f"{BASE_URL}/zh/login"
